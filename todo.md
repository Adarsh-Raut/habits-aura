streak calculating logic is wrong check it
habit not selected for that specific day should not visible on homepage but it is visible right now.
aura points system is also not working properly. check it and fix it

when there is no habit stats page show just box it should not habit create one thing

the navbar loads and loads never showing on first load. we can do one more optimization we just lazy load and show skeleton for profile thing not entire navbar this is how nextjs docs recommend it anyway.
